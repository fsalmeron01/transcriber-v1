version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: ./docker/web.Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      STORAGE_ROOT: ${STORAGE_ROOT}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      PORT: ${PORT}
      APP_URL: ${APP_URL}
      PUBLIC_DOWNLOAD_BASE_URL: ${PUBLIC_DOWNLOAD_BASE_URL}
      MAX_SOURCE_URL_LENGTH: ${MAX_SOURCE_URL_LENGTH}
      NODE_ENV: ${NODE_ENV}
    depends_on:
      - postgres
      - redis
    volumes:
      - app_data:/data
    restart: unless-stopped

  media-worker:
    build:
      context: .
      dockerfile: ./docker/media-worker.Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      STORAGE_ROOT: ${STORAGE_ROOT}
      YTDLP_FORMAT: ${YTDLP_FORMAT}
    depends_on:
      - postgres
      - redis
    volumes:
      - app_data:/data
    restart: unless-stopped

  transcribe-worker:
    build:
      context: .
      dockerfile: ./docker/transcribe-worker.Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      STORAGE_ROOT: ${STORAGE_ROOT}
      WHISPER_MODEL: ${WHISPER_MODEL}
      WHISPER_DEVICE: ${WHISPER_DEVICE}
    depends_on:
      - postgres
      - redis
    volumes:
      - app_data:/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    restart: unless-stopped
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  app_data:
  pg_data:
  redis_data:
